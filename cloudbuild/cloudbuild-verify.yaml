steps:
  - name: 'ubuntu'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "MVP verification gate placeholder"
        echo "1. Read release metadata for the candidate artifact."
        echo "2. Validate source, build, image URI, and image digest fields."
        echo "3. Confirm deployment input can reference the immutable digest."
        echo "4. Emit a verification status before any promotion step consumes the release."
        echo "See docs/cloudbuild-verification.md for the expected flow."
