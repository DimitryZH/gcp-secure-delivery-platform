steps:
  - name: 'ubuntu'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "Add release verification steps here."
        echo "Verification should validate release metadata, image digest, and deploy eligibility inputs."
        echo "See docs/cloudbuild-verification.md for MVP expectations."
